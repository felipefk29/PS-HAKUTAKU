# Arquitetura — Hakutaku MVP

> Este documento será preenchido conforme as fases avançam.

## Conteúdo previsto

1. **Diagrama de componentes** — adapters → extraction → graph → memory → reasoning → proposals → API → frontend.
2. **Fluxo de dados de uma ingestão** — passo a passo desde o documento bruto até a proposta gerada, com artefatos salvos em cada etapa.
3. **Loop de aprendizado** — como contexto recuperado da memória entra como input das extrações futuras (RAG sobre o próprio grafo).
4. **Limites e escolhas** — o que está fora do escopo do MVP (auth multi-tenant, streaming, fine-tuning, etc.) e por quê.
